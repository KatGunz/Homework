var app = angular.module("demo-app")

var ctrl = function(){
    //call service to rest call for information
    //and capture the results as 'employees'
    //let the results be displayed on html
}

app.controller('employeeCtrl', ctrl);
